name: Agent Validation

on:
  push:
    branches:
      - '*'

jobs:
  validation:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out repository code
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.11  # Use the version of Python you want

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run validation
      env: # Or as an environment variable
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        LUNARY_APP_ID: ${{ secrets.LUNARY_APP_ID }}
        SNFLK_ACCOUNT_IDENTIFIER: ${{ secrets.ACCOUNT_IDENTIFIER }}
        SNFLK_USER: ${{ secrets.USER }}
        SNFLK_PASSWORD: ${{ secrets.PASSWORD }}
        SNFLK_DATABASE: ${{ secrets.DATABASE_NAME }}
        SNFLK_SCHEMA: ${{ secrets.SCHEMA_NAME }}
        SNFLK_WAREHOUSE: ${{ secrets.WAREHOUSE_NAME }}
#        ROLE_NAME: ${{ secrets.ROLE_NAME }}
      run: python validation.py
